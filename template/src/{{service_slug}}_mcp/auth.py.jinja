"""{{ service_name }} authentication and connection setup."""

import os


def get_credentials() -> dict[str, str]:
    """Load credentials from environment variables."""
    api_key = os.environ.get("{{ service_slug | upper }}_API_KEY", "")
    return {"api_key": api_key}
